Math 
// Test jQuery.
	$('#some_content').click (this, function() {
		window.setTimeout(clicker,4*60);
		window.clearTimeout(id);
	  console.log("click_main_content");
  })
  
/*  Testing syntax Highlitening and multiline calltips,
	 seem to suit needs. See cpp.styles. */
	array
	function clicker() {
		var MyVeryLongVar = MyVeryLongVar +-* 888-0x44;
		win = window("Test").contains('code').get
		window().get(....):odd 
		$('#some_content')
		for each coll in oTest{
			$('#some_content')
		}
