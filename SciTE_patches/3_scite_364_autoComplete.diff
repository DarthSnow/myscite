diff -Nurw 3.6.4/scite_vanilla/src/SciTEBase.cxx scite_patched/src/SciTEBase.cxx
--- scite_vanilla/src/SciTEBase.cxx	2016-03-13 05:14:46.000000000 +0100
+++ scite_patched/src/SciTEBase.cxx	2016-03-24 13:08:38.000000000 +0100
@@ -1674,11 +1674,21 @@
 	if (apis) {
 		std::string words = GetNearestWords(root.c_str(), root.length(),
 			calltipParametersStart.c_str(), autoCompleteIgnoreCase);
-		if (words.length()) {
+		if (words.length() and root.length() >0 ) { 
+    // Autocomplete first called
 			EliminateDuplicateWords(words);
 			wEditor.Call(SCI_AUTOCSETSEPARATOR, ' ');
 			wEditor.CallString(SCI_AUTOCSHOW, root.length(), words.c_str());
 		}
+    if (words.length() and root.length() >0 and wEditor.Call(SCI_AUTOCACTIVE)) { 
+          //wEditor.Call(SCI_AUTOCCANCEL); 
+          EliminateDuplicateWords(words);
+          wEditor.Call(SCI_AUTOCSETSEPARATOR, ' ');
+          wEditor.CallString(SCI_AUTOCSHOW, root.length(), words.c_str());
+    } else {
+    wEditor.Call(SCI_AUTOCCANCEL);    
+    }
+    
 	}
 	return true;
 }
@@ -3973,6 +3983,7 @@
 				currentWordHighlight.textHasChanged = true;
 			}
 			//this will be called a lot, and usually means "typing".
+      StartAutoComplete();
 			EnableAMenuItem(IDM_UNDO, true);
 			EnableAMenuItem(IDM_REDO, false);
 			if (CurrentBuffer()->findMarks == Buffer::fmMarked) {
Binary files scite_vanilla/win32/DirectorExtension.o and scite_patched/win32/DirectorExtension.o differ
Binary files scite_vanilla/win32/ExportHTML.o and scite_patched/win32/ExportHTML.o differ
Binary files scite_vanilla/win32/ExportPDF.o and scite_patched/win32/ExportPDF.o differ
